# ── Kernel runtime ──
platform/kernel/moos.exe
platform/kernel/moos.exe~
platform/kernel/moos.pid
platform/kernel/data/

# ── Local workspace (never tracked) ──
CLAUDE.md
*.code-workspace
.vscode/
.claude/
.agent/
.agents/
skills/
.papers/
archive/
secrets/

# ── IDE instructions (local paths) ──
.github/copilot-instructions.md

# ── Environment & credentials ──
.env
.env.*
!.env.example
secrets/
*-service-account*.json

# ── Build / temp ──
*.log
*.tmp
*.exe~
*.pid

# ── OS ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
*.swp
*.swo
