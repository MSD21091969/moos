{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Inspect platform presets",
            "type": "shell",
            "command": "Get-ChildItem -Path moos/platform/presets",
            "options": {
                "cwd": "${workspaceFolder:FFS0_Factory}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "Inspect secrets templates",
            "type": "shell",
            "command": "Get-ChildItem -Path secrets",
            "options": {
                "cwd": "${workspaceFolder:FFS0_Factory}"
            },
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "Inspect active .agent surfaces",
            "type": "shell",
            "command": "Get-ChildItem -Path .agent/knowledge_base, .agent/knowledge, .agent/configs",
            "options": {
                "cwd": "${workspaceFolder:FFS0_Factory}"
            },
            "problemMatcher": [],
            "group": "build"
        }
    ]
}
