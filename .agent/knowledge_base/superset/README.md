# Superset Registry

## Purpose

This folder stores the machine-readable canonical registry and its derivative exports.

## Canonicality

`ontology.json` is canonical for structured typed references.
`ontology.csv` is derivative only.

## Files

- `ontology.json`
- `ontology.csv`
- `value_schema.json`
- `promotion_log.json`
