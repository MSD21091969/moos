{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Knowledge Base Value Layer Schema",
  "description": "Support schema for publication and validation of value-layer JSON artifacts. It lives in superset/ because schemas are structured registry support artifacts, while concrete contingent instances live in ../04_value_layer/.",
  "type": "object",
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "role": {
      "type": "string"
    },
    "canonicality": {
      "type": "string"
    },
    "source_refs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "schema_version",
    "role",
    "canonicality",
    "source_refs"
  ]
}