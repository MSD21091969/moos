Please think with me: As soon as kernel is running process, with all mocks conisder this 'gedankenexperiment' : You AI code assist in vscode IDE would be the missing agent, your tools would be the missing morphisms, so the graph db must me made accesible to you also (continuously?), since our current codebase is abstract, and possibly lacking essential and useable (like for the kernel program, that has them as folded 'funcs') morphism/objects, so categories but also types (as in I/O interfaces ports, fan out, rules) and projections thereof. What the industry would call 'applications'. We call them structures, or a subcategory (right term?) of it: our 'apptemplates' (could also be 'skill' or 'pdftool'). We need to think about the graph and its topology and the implementation on this workstation, think Wolfram and/or hypergraph (because structures, usergraphs, applications, they would all be projections, right ? We have to connect the kernel runtime to the graph database, effectively prepping user's rootcontainer (thats just a name, not yet materialized i think) with usergraph, i.e. all users accesible nodes or structures within. We would need to write code for actual tools, but also decice where to run them and define that in a catory, while using its runtime 'adress' in the graph. We woould need to define the (data) structures in terms of metrics (we have a piece on this in KB). We would have to name them all, that would be semantics 'over' the created syntax, this is the recursive category pattern. We need to integrate all these parts of this vision into what this mo:os application really is about: runtime programming 