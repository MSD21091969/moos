# Task: KB-Aware Boot (`--kb` flag)

**Status:** done
**Priority:** p0
**Delegated:** 2026-03-12

## Objective

Add `--kb <path>` flag so a single command boots the full kernel from a
knowledge base directory. `--config` remains as explicit override.

Given `--kb .agent/knowledge_base`, resolve:

- Registry: `<kb>/superset/ontology.json`
- Config: `<kb>/instances/distribution.json`
- Morphism log: from config or default `platform/kernel/data/morphism-log.jsonl`

## SOT References

- Design spec: `.agent/knowledge_base/design/kernel_v2_design_state_2026-03-11.md` → Layer B
- Config schema: `.agent/knowledge_base/instances/distribution.json`
- Current boot: `platform/kernel/cmd/moos/main.go`

## Acceptance Criteria

- `go run ./cmd/moos --kb .agent/knowledge_base` boots cleanly
- `--config` still works, wins if both provided
- Missing `--kb` path gives clear error
- `go test ./...` green

## Notes from Execution

- Added `config.LoadFromKB(kbRoot string) (*Config, error)` to `platform/kernel/internal/config/config.go`.
  Derives `RegistryPath = <kbRoot>/superset/ontology.json` (stat-checked), reads `StoreType` from
  `<kbRoot>/instances/distribution.json` `windows_local_dev.default_store` (best-effort), defaults to `"file"`.
  `LogPath` uses default `"data/morphism-log.jsonl"` (cwd-relative, correct when run from `platform/kernel/`).
- Updated `cmd/moos/main.go` flag parsing: switch on `--config` (wins) / `--kb` / neither (error with usage).
- Boot comment updated to reflect new dual-flag flow.
- All 7 packages pass `go test ./...` (no test files added; existing tests green).
- Verified: `.\moos.exe --kb ../../.agent/knowledge_base` boots, `/healthz` returns `{"status":"ok"}`.
- Verified: no-flag usage gives `usage: moos --config <path> | --kb <kb-root>` exit 1.
- Verified: bad `--kb` path gives clear error with the missing path in message.
