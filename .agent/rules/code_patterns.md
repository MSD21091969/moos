# Code Patterns

> Foundational patterns — pending new model version

---

## Status

**Current state**: `models_v2/` and `parts/` are legacy, awaiting versioning.

**Next**: Define new `models/` and `sdk/` with clean patterns.

---

## Core Concepts (to be defined)

| Concept    | Purpose                           |
| ---------- | --------------------------------- |
| Container  | Workspace context holder in graph |
| Definition | Versioned I/O contract            |
| Graph      | Topology of nodes                 |

---

## Versioning Approach

See: `knowledge/architecture/versioning.md` (to be created)
