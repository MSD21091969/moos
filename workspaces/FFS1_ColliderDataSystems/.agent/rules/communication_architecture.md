# Communication Architecture

- FFS3 to backend API compatibility via `:8000`.
- Agent session compatibility via `:8004`.
- WebSocket bridge compatibility via `:18789`.
- Tool/MCP surface via `:8001`.
