# Stack Standards

- Backend compatibility runtime: MOOS (`apps/data-server`, `apps/tool-server`, `apps/engine`).
- Frontend runtime: FFS3 Nx (Vite + React 19).
- Keep contracts aligned to ports `8000`, `8001`, `8004`, `18789`, `4200-4202`.
