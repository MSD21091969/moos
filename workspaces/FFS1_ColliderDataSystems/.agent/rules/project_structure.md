# Project Structure

- FFS1: governance + shared contracts.
- FFS2: backend compatibility runtime (`moos`).
- FFS3: frontend apps (`ffs4`, `ffs5`, `ffs6`).
- Child app workspaces may carry minimal `.agent` stubs.
