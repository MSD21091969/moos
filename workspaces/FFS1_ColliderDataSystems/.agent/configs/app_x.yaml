# App X Configuration (FILESYST Domain)

application:
  id: "app_x"
  name: "Collider IDE"
  domain: "filesyst"
  
backend:
  type: "native_messaging"
  host: "com.collider.agent_host"

# All features available - admin manages settings
# Configuration model to be defined later
