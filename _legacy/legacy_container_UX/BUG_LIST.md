# Bug List

**Last Updated:** 2025-12-15
**Current Cycle:** Phase 1 (UX/Demo)

## Phase 1 Status: 🟡 IN PROGRESS

| ID | Description | Status | Priority | Owner | Notes |
|----|-------------|--------|----------|-------|-------|
| P1-01 | "Add Existing Agent" picker fails to open/render | 🟡 In Progress | High | Copilot | `loadOrphans()` loads orphans before picker opens. **Needs E2E test.** |
| P1-02 | "Add Existing Tool" picker fails to open/render | 🟡 In Progress | High | Copilot | Same as P1-01. `getOrphansByType()` filters registry. **Needs E2E test.** |
| P1-03 | Drag & Drop Nesting not implemented | 🔴 Open | Medium | Copilot | Confirmed. No visual feedback or logic for nesting. |
| P1-04 | Double-click Navigation | ✅ Fixed | High | Copilot | Test passed unexpectedly. Code review suggests handler is missing, but behavior is correct. |
| P1-05 | "Run Agent" unavailable in Demo Mode | 🚫 Won't Fix | Low | Copilot | By design (demo mode). |
| P1-06 | Root Workspace Context Menu missing | 🔴 Open | High | Copilot | Cannot create items (Agent/Tool) at root level because `activeContainerId` is null. |
| P1-07 | Backend orphan-only adoption enforcement | 🟡 In Progress | High | Copilot | `add_resource` raises `ValidationError` for non-orphans. **Needs integration test.** |
| P1-08 | SSE handler missing parent_id tracking | 🟡 In Progress | Medium | Copilot | CASE 2 added in `handleContainerEvent`. **Needs E2E test.** |
| P1-09 | Mock backend orphan support | 🟡 In Progress | Medium | Copilot | Implemented demo orphans (`ORPHAN_AGENT`, `ORPHAN_TOOL` with `parent_id: null`). **Needs E2E verification:** "Add Existing..." shows orphans in Demo Mode. |
| P1-10 | Fast UX capture workflow too slow/brittle | 🟡 In Progress | High | Copilot | Implemented Option B recorder (`frontend/scripts/mcp/record-ux.ts`). **Needs regression verification:** run golden-path task and confirm JSONL+summary output + useful events. |
| P1-11 | CDP tab/port drift (5173 vs 5174) breaks tooling | 🟡 In Progress | High | Copilot | Defaults aligned to `http://localhost:5174/workspace` in `.vscode/settings.json`, `scripts/launch-edge.ps1`, prompt/tasks. **Needs verification:** recorder attaches to correct tab consistently. |
| P1-12 | Phase 1 launcher prompts for web parsing (blocks tasks) | 🟡 In Progress | Medium | Copilot | Implemented non-interactive readiness polling in `scripts/start-phase1.ps1`. **Needs verification:** task runs end-to-end without PowerShell prompts. |
| P1-13 | Missing System Definitions (Backend) | 🔴 Open | Medium | Copilot | Backend has no system definitions seeded (unlike frontend DEMO_AGENT_DEFINITIONS). Phase 1 uses mock frontend data, so non-blocking for UX testing. Phase 3 needs seeded definitions. **TODO:** Create `backend/scripts/development/seed_system_definitions.py` to populate `/agent_definitions/`, `/tool_definitions/`, `/source_definitions/` in Firestore. Reference: [ARCHITECTURE.md §6.2](docs/ARCHITECTURE.md#62-backend-architecture) |
| P1-14 | Structural UX Guards (Permissions & Rules) | 🔴 Open | High | Copilot | **Grid:** Object color/pos driven by `ResourceLink`. **Menus:** Filtered by Depth/Tier/Registry. **Add Existing:** Only show Orphans (`parent=None`). |
| P1-15 | Remove Demo DebugOverlay (bottom-left "Runtime State" terminal HUD) | 🔴 Open | Medium | Copilot | Old/noisy; obscures UX and pollutes recordings/screenshots. Currently forced in Demo Mode via `isDemoMode() && <DebugOverlay />` in the workspace page. |
| P1-16 | Demo crash: Create Session inside Tool → workspace blank screen | 🔴 Open | High | Copilot | **Repro:** Workspace → Q4 Sales Analysis → Excel Parser → right-click canvas → Create Session → click header brand to go back to `/workspace`. **Observed:** blank canvas; React error boundary suggestion. **Console:** `RangeError: Maximum call stack size exceeded` while rendering workspace root. Likely recursion loop in [frontend/src/components/GameCanvas.tsx](frontend/src/components/GameCanvas.tsx#L156-L179) (`calculateDepth` has no cycle guard). **Also seen:** `Failed to sync node position: Error: Resource not found after update` thrown in [frontend/src/lib/api.ts](frontend/src/lib/api.ts#L472-L491) (called from [frontend/src/lib/store/canvas-slice.ts](frontend/src/lib/store/canvas-slice.ts#L131-L191)). **Evidence:** UX recorder JSONL: [frontend/test-results/mcp/ux-20251215-160622.jsonl](frontend/test-results/mcp/ux-20251215-160622.jsonl) + [frontend/test-results/mcp/ux-20251215-181131.jsonl](frontend/test-results/mcp/ux-20251215-181131.jsonl). |
| P1-17 | Demo: “Create Session” often doesn’t appear (L0/L1/L2) | 🔴 Open | High | Copilot | **Observed:** creating sessions at multiple levels (L0 workspace root, L1 inside a session, L2 inside a tool) is unreliable: session node often does not appear after “Create Session”. **Control:** adding non-session containers is not consistently affected. **Impact:** blocks building session hierarchy during UX testing even when it doesn’t crash. |
| P1-18 | Runtime type drift: activeContainerType reports “session” in tool context | 🔴 Open | High | Copilot | **Observed:** recorder snapshot shows `activeContainerId=session-1-tool` while `activeContainerType=session` during Tool navigation (Excel Parser / Flight Search paths). **Impact:** rules/gating can be applied using the wrong type, and “Create Session” can attach to the wrong parent. **Code:** menu gating uses `activeContainerType` ([frontend/src/components/ContextMenu.tsx](frontend/src/components/ContextMenu.tsx#L157-L175)); session creation uses `parentId = activeSessionId || userSessionId` rather than `activeContainerId` ([frontend/src/components/ContextMenu.tsx](frontend/src/components/ContextMenu.tsx#L182-L206)). **Evidence:** [frontend/test-results/mcp/ux-20251215-181131.jsonl](frontend/test-results/mcp/ux-20251215-181131.jsonl). |
| P1-19 | Spec drift / decision needed: Sessions as context containers under Agent/Tool | 🔴 Open | Medium | Copilot | **Question:** should sessions be creatable under Agent/Tool (as “context containers”), or only under Session? **Current:** UX shows Session creation reachable in Tool context (and can trigger P1-16). **Next:** confirm intended containment rules (then align UI gating + backend validation + tests). |

## Phase 2 Status: 🔴 BLOCKED (By Phase 1)

| ID | Description | Status | Priority | Owner | Notes |
|----|-------------|--------|----------|-------|-------|
| P2-01 | Standalone Container Creation | 🔴 Blocked | High | Copilot | Decouple instantiation from linking. Currently, creation is architecturally coupled to resource linking, which enforces a parent immediately. Need to support creating "orphaned" containers for the "Add Existing" pool. Reference: [ARCHITECTURE.md §11.2](docs/ARCHITECTURE.md#112-high-priority) |
| P2-02 | Definition Versioning | 🔴 Blocked | High | Copilot | No migration path when definitions change. Need ADR + versioning strategy. Reference: [ARCHITECTURE.md §11.2](docs/ARCHITECTURE.md#112-high-priority) |
| P2-03 | Custom Definitions UI | 🔴 Blocked | High | Copilot | PRO/ENT users cannot create custom definitions yet. Backend service exists (`definition_service.py`) but frontend picker/form missing. Reference: [ARCHITECTURE.md §11.2](docs/ARCHITECTURE.md#112-high-priority) |
