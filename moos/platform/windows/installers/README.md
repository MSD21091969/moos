# Windows Installers

Store Windows packaging assets here.

Examples:

- `install.exe`
- MSI/WiX manifests
- signing notes
- release checksums
- bootstrap scripts that hydrate a `platform/presets/*.json` profile

Do not add an installer binary without version metadata or a reproducible build note.
